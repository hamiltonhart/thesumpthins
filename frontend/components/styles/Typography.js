import styled from "styled-components";

export const PageHeadingText = styled.h1`
  font-size: var(--huge);
  text-align: center;
  color: var(--primary);
  font-weight: 600;
  text-transform: uppercase;
`;
